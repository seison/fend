import { execute } from '../client/js/init';

describe('check the execute function definition', () => {
    test('execute function to be defined', () => {
        expect(execute).toBeDefined();
    });
});
