import { handleSubmit } from '../client/js/formHandler';

describe('check the "handleSubmit" function definition', () => {
    test('"handleSubmit" function to be defined', () => {
        expect(handleSubmit).toBeDefined();
    });
});
